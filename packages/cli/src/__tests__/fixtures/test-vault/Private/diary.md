# March 14th

No frontmatter. Sits inside `Private/`.

When `excludedFolders: ["Private"]` is set, this note is silently skipped.
It has no `publish: true` override, so it stays off the web.

Personal reflections that aren't meant for public eyes.
