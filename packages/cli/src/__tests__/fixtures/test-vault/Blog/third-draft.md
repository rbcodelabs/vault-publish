---
publish: false
title: Third Draft (Private)
---

# Third Draft

This post isn't ready yet. Even though it lives in the `Blog/` folder,
`publish: false` is an absolute veto — it won't be included even when
`Blog` is in `includedFolders`.

Half-baked ideas live here.
